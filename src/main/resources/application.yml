server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservice
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver  # Corrected indentation

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  spring:
   jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
